apiVersion: apps/v1
Kind: Deployment
metada:
  name: my-deployment
  labels: 
    app: my-deployment
spec: 
  replicas: 2
  selector:
    matchlabels: 
      app: my-deployment
  template: 
    metadata: 
      labels:
        app: my-deployment
    spec: 
      containers:
      - name: my-deployment 
        image: rishikanth00/project2
        ports:
        - containerPort: 80
